# Plan: Redesign Smara CLI Desktop Frontend with Modern Dark Mode via Stitch MCP

## Context
- Target: Wails v2 desktop frontend (vanilla TypeScript + CSS) at `smara-desktop/frontend/`
- Existing Stitch project: `Smara Desktop Redesign` (ID: `5691918552833023076`) with pre-configured dark mode design system ("Ethereal Void": dark mode, Inter font, lavender #E6E6FA accent, glassmorphism, no-message-bubble rules)
- No screens exist in the Stitch project yet; existing frontend has light/dark theme support with indigo accents

## Steps

### 1. Verify Stitch Design System
- Confirm existing design system matches modern dark mode requirements (already aligned: dark color mode, Inter font, ROUND_FOUR roundness, lavender accent, glassmorphism rules)
- Adjust via `stitch_update_design_system` only if user requests changes

### 2. Generate Stitch Screens for All Frontend Components
Use `stitch_generate_screen_from_text` with `projectId: 5691918552833023076`, `deviceType: DESKTOP`:
1. **Main App Layout Screen**
   - Prompt: "Modern dark mode Wails desktop app main layout. Left 240px sidebar with workspace list, session history, tools inspector, settings button. Right main content with header (workspace name, theme toggle, model indicator), chat area (no message bubbles, whitespace typography hierarchy), bottom input area (text field + send button). Glassmorphism, lavender #E6E6FA accents, Inter font, #131313 dark background. Preserve element IDs: #sidebar, #chat-input, #send-btn, #session-list, #workspace-list"
2. **Settings Modal Screen**
   - Prompt: "Dark mode settings modal overlay for Wails app. Glass panel with 30px backdrop blur, fields for LLM provider select, model input, API key input, base URL input, save button. Lavender accents, Inter font, matches Ethereal Void design system. Preserve ID: #settings-modal"
3. **Sidebar Component Screen**
   - Prompt: "Dark mode sidebar for Wails app, 240px wide, dark glass background, nav items for workspaces/sessions/tools/settings. Active state with 2px lavender vertical line, Inter font, no borders, glassmorphism. Preserve ID: #sidebar"

### 3. Review Generated Screens
- List screens via `stitch_list_screens`
- Fetch screen images via `stitch_fetch_screen_image` to validate design
- Fetch生产就绪 code via `stitch_fetch_screen_code` for all screens

### 4. Integrate Stitch Code into Wails Frontend
- Replace `smara-desktop/frontend/src/style.css` with adapted Stitch-generated CSS
- Update `smara-desktop/frontend/index.html` with Stitch HTML structure, preserving Wails bindings and element IDs
- Adjust `smara-desktop/frontend/src/main.ts` only if DOM structure changes require it

### 5. Validate
- Run `wails dev` in `smara-desktop/` to test new design
- Verify dark mode renders correctly, all interactive elements function as before

## Key References
- Existing frontend files: `smara-desktop/frontend/src/style.css`, `smara-desktop/frontend/index.html`, `smara-desktop/frontend/src/main.ts`
- Stitch project ID: `5691918552833023076`
- Design system: "Ethereal Void" (dark mode, Inter font, lavender accent, glassmorphism)
